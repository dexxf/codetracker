package com.io.kira.application.classroom.error;

public enum CreateClassroomError {
    INVALID_INSTRUCTOR,
    USERID_NOT_FOUND,
    INVALID_NAME,
    INVALID_DESCRIPTION,
    INVALID_MAX_STUDENTS,
    INVALID_PASSCODE;
}
